services:
  web:
    build: .
    container_name: cinerec
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    env_file: .env
    environment:
      - TMDB_API_KEY=${TMDB_API_KEY:-}
    restart: unless-stopped
