version: '3.8'

services:
  cinerec:
    build: .
    container_name: cinerec
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - TMDB_API_KEY=${TMDB_API_KEY:-}
    restart: unless-stopped
